# 開発ログ 2026-02-19 #001

## 実行したこと
- Step 0「開発環境構築」に着手
- `docker-compose.yml` を作成し、PostgreSQL 17 を起動
- `psql` で DB 接続を確認 → OK

## 詰まったところ
- `services` のタイポ (`servies`)
- `ports` / `volumes` のリスト形式（`-` 付き）の書き方
- トップレベル `volumes` の定義が必要なことを知らなかった

## 学び
- 名前付きボリュームをつかうときはトップレベルに定義→サービス内で記述する必要あり
- リストの書き方は慣れかも。services内は基本リスト。

## 次回着手すること
- Step 0 の残り: backend / frontend の初期化 → ESLint + Prettier → devcontainer
