# 開発ログ 2026-02-24 #001

## 実行したこと
- frontend に Prettier (`prettier` + `eslint-config-prettier`) を導入
- `.prettierrc` を backend と同じルールで作成 (`semi`, `singleQuote`, `tabWidth: 2`)
- `eslint.config.js` に `eslint-config-prettier` を追加（配列の末尾に配置）
- `npx prettier --write .` でフォーマット自動修正を実行
- backend の `eslint.config.mjs` を frontend と同じスタイルにリファクタリング（`extends` にオブジェクト直接渡し、`plugins` + 文字列参照を廃止）

## 詰まったところ
- `npx prettier --write .` で README.md など意図しないファイルもフォーマットされた → `.prettierignore` で制御できることを確認

## 学び
- eslint.config書き方が多い みんな理解してるんかこれ

## 次回着手すること
- devcontainer の設定を作成する（`.devcontainer/devcontainer.json`、既存の `docker-compose.yml` との連携、VS Code 拡張の設定）
- Step 0 を完了させて Step 1 へ進む
